# 变更记录

## 2026-08-13 · v0.2.0（升级包）

本版本聚焦三件事：推送/退出稳定性、部署兼容性、工程化配套。

### 稳定性

- 钉钉推送（含退避重试）改为线程池异步执行，不再阻塞 asyncio 事件循环；
- 新增退出看门狗与推送中断信号：收到 SIGTERM/SIGINT 后限时优雅退出，
  不再等完最多约 30 秒的重试退避；
- 修复线程池 Future 唤醒回调在信号竞态下丢失、协程永久挂起的问题。

### 新功能

- 开机状态播报（`STARTUP_NOTIFY`，默认开启）：启动时推送系统指标 +
  服务 UP/DOWN/SKIP 汇总，并与 SKIP 一次性通知去重；
- 日志语义诊断规则可配置化（`MONITOR_DIAGNOSTICS`，默认内置
  Nginx 网关过载 / Docker OOM 两条）。

### 正确性修复

- 进程名匹配由“子串包含”改为“精确 / 通配符匹配”，避免 `nginx` 误匹配 `nginx-foo`；
- 环境变量非法数值不再于 import 阶段直接崩溃，改为启动时统一友好报错；
- Windows 等无 `AF_UNIX` 平台的 socket 探测兼容。

### 部署

- install.sh 自动探测 `python3` 路径写入服务单元（不再写死 `/usr/bin/python3`）；
- 检测 systemd 版本，< 235（如 CentOS 7）自动改用 legacy 兼容模板；
- 安装目录安全护栏：禁止把安装位置设为主目录、根目录或源码仓库（含子目录）；
- 备份时间戳追加进程号，避免同一秒重复安装覆盖备份。

### 工程化

- 新增 pytest 单测 22 例（不依赖真实系统资源，本地/CI 均可跑）；
- 新增 ruff / mypy 配置与 GitHub Actions CI（Python 3.8 / 3.10 / 3.12 矩阵）。
